services:
  # Ton moteur RAG
  backend-rag:
    build: ./backend
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
    volumes:
      - ./backend/data:/app/data

  # Ton interface utilisateur
  frontend-ui:
    build: ./frontend/rag-afrique-hub
    ports:
      - "5173:80"
    depends_on:
      - backend-rag